(1)make sure that global-heap is installed to /usr/local/bin

(2)create a global heap named "/dev/shm/nvm/global0" via
  globalheap-util create /dev/shm/nvm/global0 --size=32G

(3)then invoke each of the test scripts. 

